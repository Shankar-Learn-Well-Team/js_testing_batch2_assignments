/**
 * Add fruit to the end of array
 * @param {array} fruits
 * @param {string} newFruit
 * @returns array with fruit add at last
 */
function addFruitToArray(fruits, newFruit) {
    fruits.push(newFruit);
    return fruits;
}
