// Creating a map using Map object
let newMap = new Map();

// Adding values to the map
newMap.set("apple",'a fruit' );
newMap.set("banana",'a tropical fruit');

// Logging map object to console
newMap.forEach((values, keys) => {
	console.log(values, keys);
});

 